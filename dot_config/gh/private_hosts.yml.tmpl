github.com:
    users:
        devsprithvi:
            oauth_token: {{ output "infisical" "secrets" "get" "ADMIN_PAT" "--env" "global" "--path" "/github" "--plain" | trim }}
    git_protocol: https
    oauth_token: {{ output "infisical" "secrets" "get" "ADMIN_PAT" "--env" "global" "--path" "/github" "--plain" | trim }}
    user: devsprithvi
